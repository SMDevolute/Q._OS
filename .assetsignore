# Files under the assets directory (repo root) that should NOT be served as static assets.
# The app files (index.html, *.dc.html, support.js, assets/, acquire/, close/, retain/, docs/) are served.
.git
.github
.wrangler
node_modules
src
wrangler.toml
.assetsignore
.gitignore
